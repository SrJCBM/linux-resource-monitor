# linux-resource-monitor
A lightweight Linux resource monitoring application built with Python. It collects CPU, memory, disk, network, process, and user information using `/proc` and native Linux commands, and stores monitoring snapshots through CRUD operations.
